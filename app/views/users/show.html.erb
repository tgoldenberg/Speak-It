<div class="stats-wrapper">
	<h2 class="stats-header">
		<img class="avatar" src="<%= current_user.avatar_url %>"/>
		<b><%= current_user.username.titleize %></b>: User Stats
	</h2>
	<%= react_component('Stats', @data_hash, class: "stats-page") %>
</div>